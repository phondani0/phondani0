name: GitHub Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # ----------------------------------------------------
          # TOKENS
          # ----------------------------------------------------
          token: ${{ secrets.METRICS_TOKEN }}
          
          # ----------------------------------------------------
          # CORE SETUP (This creates the two-column wide layout)
          # ----------------------------------------------------
          user: ${{ github.repository_owner }}
          template: classic
          base: header, metadata
          config_timezone: Asia/Kolkata
          
          # THIS IS THE MAGIC WORD FOR THE TWO-COLUMN WIDE LAYOUT
          config_display: large 
          
          # ----------------------------------------------------
          # LEFT / MIDDLE COLUMN PLUGINS
          # ----------------------------------------------------
          # Coding habits and recent activity
          # plugin_habits: yes
          # plugin_habits_from: 200
          # plugin_habits_days: 14
          # plugin_habits_facts: yes
          # plugin_habits_charts: yes

          # Mastered technologies and topics (Icons)
          plugin_topics: yes
          plugin_topics_mode: starred
          
          # ----------------------------------------------------
          # RIGHT COLUMN PLUGINS
          # ----------------------------------------------------
          # PageSpeed Insights
          plugin_pagespeed: yes
          plugin_pagespeed_token: ${{ secrets.PAGESPEED_TOKEN }}
          plugin_pagespeed_url: https://ankitphondani.com
          
          # Isometric calendar
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          
          # Suggested tracks (Music)
          plugin_music: yes
          plugin_music_provider: spotify 
          plugin_music_mode: playlist
          plugin_music_playlist: https://open.spotify.com/playlist/37i9dQZF1DWWxPM4nWdhyI
          
          # Most used languages
          plugin_languages: yes
          plugin_languages_ignored: html, css, jupyter notebook
          plugin_languages_recent_days: 900
          plugin_languages_details: percentage
          plugin_languages_indepth: yes 
          
          # Projects
          plugin_projects: yes
          plugin_projects_limit: 1
